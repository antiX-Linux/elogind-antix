#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ "$1" = "configure" ] || [ "$1" = "abort-upgrade" ]; then
	if [ -x "/etc/init.d/elogind" ]; then
		update-rc.d elogind defaults >/dev/null
		invoke-rc.d elogind start || exit $?
	fi
fi
# End automatically added section
